#!/bin/bash

dns_ip=192.168.122.1
bridge=virbr0
domain=example.com

echo DNS IP: $dns_ip
echo Bridge: $bridge
echo Domain: $domain

sudo resolvectl dns $bridge $dns_ip
sudo resolvectl domain $bridge ~$domain
